import { AmTimeAgoPipe } from './am-time-ago.pipe';

describe('AmTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new AmTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
