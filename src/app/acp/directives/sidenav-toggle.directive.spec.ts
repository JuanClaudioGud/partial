import { SidenavToggleDirective } from './sidenav-toggle.directive';

describe('SidenavToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavToggleDirective();
    expect(directive).toBeTruthy();
  });
});
