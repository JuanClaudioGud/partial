import { BookingTriggerDirective } from './booking-trigger.directive';

describe('BookingTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
