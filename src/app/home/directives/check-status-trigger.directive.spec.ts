import { CheckStatusTriggerDirective } from './check-status-trigger.directive';

describe('CheckStatusTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckStatusTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
